<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icono"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icono"
        android:supportsRtl="true"
        android:theme="@style/Theme.SimuladorAppToApp"
        tools:targetApi="31">

        <!-- Splash Screen Activity (primera pantalla) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity (dashboard principal) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.SimuladorAppToApp" />

        <!-- VentaActivity -->
        <activity
            android:name=".VentaActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".DetalleVentaActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".AnulacionActivity"
            android:exported="false" />
        <activity
            android:name=".DevolucionActivity"
            android:exported="false" />
        <activity
            android:name=".DuplicadoActivity"
            android:exported="false" />
        <activity
            android:name=".CierreActivity"
            android:exported="false" />
        <activity
            android:name=".PrintServicesActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

    </application>

</manifest>